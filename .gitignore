.venv/
__pycache__/
*.pyc
dist/
build/
*.egg-info/
models/

# Generated by the design skill: the artboards under design/*.dc.html are the
# source, this is a 2.8 MB bundle built from them.
design/omavoi-interface.html

# Created by `uv run`/`uv sync` for development. `uv tool install
# git+...`, which is how the daemon is actually installed, resolves fresh and
# never reads it, so committing it would only pin dev environments.
uv.lock
